@*@model List<SiteManagementProject.Models.Menu>
    @{
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

    <h1>Category List</h1>
    <br />
    @using (Html.BeginForm())
    {
        <p>
            Search:@Html.TextBox("p")
            <input type="submit" value="Find" />
        </p>
    }
    <table class="table table-bordered">
        <tr>
            <th>ID</th>
            <th>Menu Name</th>
            <th>Status</th>
            <th>Delete</th>
            <th>Update</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.MenuID</td>
                <td>@item.Name</td>
                <td>@item.Status</td>
                <td><a href="/Category/CategoryDelete/@item.MenuID" class="btn btn-danger">Delete</a></td>
                <td><a href="/Category/CategoryGet/@item.MenuID" class="btn btn-success">Update</a></td>

            </tr>
        }
    </table>
    <a href="/Category/CategoryAdd/" class="btn btn-primary">New Category</a>*@


@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_SiteLayout.cshtml";
}